# Example auth-path file

This file lives under `docs/auth/` purely to exercise the `**/auth/**`
high-risk classifier rule during a live end-to-end validation PR. It will
be removed once validation is done — this PR is not meant to be merged.
