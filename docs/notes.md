# Example docs file (updated)

This file lives under `docs/` purely to exercise the `docs/**` low-risk
classifier rule during a live end-to-end validation PR. It will be removed
once validation is done — this PR is not meant to be merged.
